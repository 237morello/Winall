/**
 * Mission : Composant de la page d'accueil : hero-s est un alias temporaire vers la nouvelle feature Hero.
 */
import { SectionHero } from "@/components/features/accueil/Hero";

export default function HeroS() {
  return <SectionHero />;
}
