/**
 * Mission : Composant de la page d'accueil : header-s est un alias temporaire vers la nouvelle feature Header.
 */
import { Header } from "@/components/features/Header";

export default function HeaderS() {
  return <Header />;
}
